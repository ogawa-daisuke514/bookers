<p id="notice">
<% if flash[:notice] != nil %>
<%= flash.now[:notice] %>
<% end %>
<p>
  <strong>Title:</strong><%= @book.title %>
</p>
<p>
  <strong>Body:</strong><%= @book.body %>
</p>
<%= link_to "Edit", edit_book_path(@book.id) %> |
<%= link_to "Delete", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure to delete [#{@book.title}]?" %> |
<%= link_to "Back", books_path %>